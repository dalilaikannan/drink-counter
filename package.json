{
  "name": "js-trial",
  "version": "1.0.0",
  "main": "main.js",
  "scripts": {
    "start": "concurrently \"npm start --prefix app\" \"wait-on http://localhost:3000 && electron .\"",
    "test": "echo \"Error: no test specified\" && exit 1",
    "electron-build": "npm run build --prefix app && electron ."
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "devDependencies": {
    "electron": "^34.3.0"
  },
  "dependencies": {
    "concurrently": "^9.1.2",
    "cross-env": "^7.0.3",
    "preload.js": "^1.0.0",
    "wait-on": "^8.0.3"
  }
}
